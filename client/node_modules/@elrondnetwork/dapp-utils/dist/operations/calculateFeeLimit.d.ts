interface CalculateFeeLimitType {
    minGasLimit: string;
    gasLimit: string;
    gasPrice: string;
    data: string;
    gasPerDataByte: string;
    gasPriceModifier: string;
    chainId: string;
}
export default function calculateFeeLimit({ minGasLimit, gasLimit, gasPrice, data: inputData, gasPerDataByte, gasPriceModifier, chainId, }: CalculateFeeLimitType): string;
export {};
